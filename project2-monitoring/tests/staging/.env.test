# Testing Environment Variables
# DO NOT USE IN PRODUCTION - USE ../.env INSTEAD

# NetBox Configuration (test instance)
NETBOX_HOST=localhost
NETBOX_PORT=8000
NETBOX_TOKEN=0123456789abcdef0123456789abcdef01234567
NETBOX_SCHEME=http
NETBOX_VERIFY_SSL=False

# InfluxDB Configuration (test instance)
INFLUX_HOST=localhost
INFLUX_PORT=8086
INFLUX_BUCKET=device_health_test
INFLUX_ORG=netdevops
INFLUX_TOKEN=test_token_do_not_use_in_production
INFLUX_VERIFY_SSL=False

# Polling Configuration
POLL_INTERVAL=30

# Demo Mode (always enabled in testing)
DEMO_MODE=true
